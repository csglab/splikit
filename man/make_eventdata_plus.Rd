% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/star_solo_processing.R
\name{make_eventdata_plus}
\alias{make_eventdata_plus}
\title{make_eventdata_plus}
\usage{
make_eventdata_plus(eventdata, GTF_file_direction)
}
\arguments{
\item{eventdata}{A data table of genomic intervals for events. Must contain columns:
\itemize{
\item \code{chr}: Chromosome name (e.g., "chr1").
\item \code{start}: Start coordinate of the event.
\item \code{end}: End coordinate of the event.
\item \code{strand}: Numeric strand indicator (\code{1} or \code{2}).
}}

\item{GTF_file_direction}{A character string specifying the path to a GTF file. The file
must contain at least these columns: \code{seqid}, \code{start}, \code{end}, \code{strand}, \code{gene_id},
and \code{gene_name}.}
}
\value{
A data table containing overlapping event intervals with added gene metadata
(such as \code{gene_id} and \code{gene_name}). The columns returned will include both event-level
and gene-level information.
}
\description{
This function reads in a GTF file, extracts gene annotations, and merges them with
event-level genomic intervals provided by the user. The final data table contains the
original event intervals and the corresponding gene information (for example, \code{gene_id}
and \code{gene_name}).
}
\details{
\enumerate{
\item Read the GTF: Uses \code{data.table::fread} to load GTF data and convert it to
a data table.
\item Subset for Genes: Keeps only rows where \code{type == "gene"}, retaining columns
for chromosome, start, end, strand, gene_id, and gene_name.
\item Strand Conversion: Merges the GTF data with a small lookup table to replace
\code{+} and \code{-} with numeric strand indicators \code{1} and \code{2} (matching STAR).
\item Overlaps: With both data sets keyed, uses \code{foverlaps()} from \code{data.table} to
find intervals in \code{eventdata} that fall fully within gene boundaries.
}
}
